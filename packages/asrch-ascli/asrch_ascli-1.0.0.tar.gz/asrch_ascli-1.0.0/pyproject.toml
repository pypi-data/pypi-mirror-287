[tool.poetry]
name = "asrch_ascLI"
version = "1.0.0"
description = "A CLI formatting tool"
authors = ["j"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
