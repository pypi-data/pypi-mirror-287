from list.count import elemcount
def count(d, val):
    if type(d) == type(dict()):
        return [v for k, v in d.items()].count(val)
    return None
def valuecount()
    if type(d) == type(dict()):
        return elemcount([v for k, v in d.items()])
    return None
