[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm[toml]>=6.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "modmon"
version = "0.0.0-alpha.7"
authors = [{ name = "Karam Ravindra", email = "contact@karanravindra.me" }]
description = "A self-host first performant ml-ops platform"
readme = "README.md"
