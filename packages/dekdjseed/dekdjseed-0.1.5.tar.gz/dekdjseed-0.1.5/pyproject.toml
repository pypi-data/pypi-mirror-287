[project]
name = "dekdjseed"
version = "0.1.5"
description = ""
authors = [
    { name = "sanzenwin", email = "sanzenwin@gmail.com" },
]
dependencies = [
    "Faker<=2.0.1",
    "toposort<=1.7",
    "dekdjtools<1.0.0",
]
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[tool.pdm.build]
includes = [
    "dekdjseed",
]

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"
