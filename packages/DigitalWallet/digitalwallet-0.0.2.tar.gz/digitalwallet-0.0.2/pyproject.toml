[tool.poetry]
name = "DigitalWallet"
version = "0.0.2"
description = "DigitalWallet is an async service that counts your currencies."
authors = ["heliosgrounder"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8,<3.12"
https = "0.27.0"
fastapi = "0.111.1"
pydantic = "2.8.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"