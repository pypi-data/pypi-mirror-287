version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: latest
  commands:
    - pip install sphinx myst_nb sphinx_book_theme
    - sphinx-build -b html ./doc/source ./doc/build/html
    - mkdir --parents $READTHEDOCS_OUTPUT
    - mv doc/build/html $READTHEDOCS_OUTPUT/html
