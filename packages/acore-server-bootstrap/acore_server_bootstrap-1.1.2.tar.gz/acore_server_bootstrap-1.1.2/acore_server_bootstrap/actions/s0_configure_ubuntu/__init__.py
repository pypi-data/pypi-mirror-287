# -*- coding: utf-8 -*-

"""
这一步是配置 EC2 服务器的第一步, 配置 Ubuntu 系统. 其中具体步骤包括

1. 关闭 Ubuntu 的自动更新. 主要是为了防止 Ubuntu 更新 MySQL Client 的小版本. 因为游戏服务器
    要求 MySQL Client 的版本跟服务器编译时的版本以及数据库的版本一摸一样.
2. 创建每次 EC2 重启后自动运行的脚本. 因为我们希望每次启动服务器后自动运行 "服务器健康状态监控脚本"
    以及 "服务器自动启动脚本".

.. note::

    这个步骤中的所有操作都需要 sudo 权限.
"""
