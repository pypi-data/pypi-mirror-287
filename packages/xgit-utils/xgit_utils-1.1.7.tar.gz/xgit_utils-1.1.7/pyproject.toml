[project]
name = "xgit-utils"
description = "git utils"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT License" }
authors = [{ name = "Seven", email = "1162584980@qq.com" }]
dynamic = ["version", "dependencies"]

classifiers = [
    "Topic :: Utilities",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]

[project.scripts]
xclone = "gitutils.gitclone:execute"
xsync = "gitutils.gitsync:execute"
xgithub = "gitutils.github:execute"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = { attr = "gitutils.version.VERSION" }
dependencies = { file = ["requirements.txt"] }

[tool.setuptools.packages.find]
include = ["gitutils*"]

#[tool.black]
#extend_exclude = "gitutils/contrib/"
