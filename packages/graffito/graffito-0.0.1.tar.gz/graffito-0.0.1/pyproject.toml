[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "graffito"
version = "0.0.1"
dependencies = []

readme = "README.md"
description = "A function-tagging library using decorators as tags."
